---
import type { CollectionEntry } from "astro:content";
import { projectLinkKind } from "../../content/project-links";
import ProjectLinkIcon from "./ProjectLinkIcon.astro";
interface Props {
  project: CollectionEntry<"projects">;
}
const { project } = Astro.props;
const { title, summary, tags, badge, media: preview, links } = project.data;
---

<li
  class="project-tile"
  data-project-id={project.id}
  data-project-tags={tags.map((tag) => tag.toLowerCase()).join("|")}
>
  <article class="project-card">
    <div class="project-card__heading">
      <h2>{title}</h2>
      <div
        class="project-card__links"
        role="group"
        aria-label={`${title} links`}
      >
        {
          links.map((link) => (
            <a
              href={link.href}
              target="_blank"
              rel="noreferrer"
              aria-label={`${link.label} — opens in a new tab`}
              data-label={link.label}
            >
              <ProjectLinkIcon kind={projectLinkKind(link.href, link.kind)} />
            </a>
          ))
        }
      </div>
      <span
        class="project-card__drag-handle"
        data-project-drag-handle
        aria-hidden="true"
      ></span>
    </div>
    <div class="project-card__media">
      <div
        class:list={[
          "project-card__preview",
          !preview && "project-card__preview--empty",
        ]}
      >
        {
          preview?.type === "video" ? (
            <video
              src={preview.src}
              poster={preview.poster}
              aria-label={preview.alt}
              muted
              loop
              playsinline
              preload="metadata"
            />
          ) : preview?.type === "image" ? (
            <img
              src={preview.src}
              alt={preview.alt}
              loading="lazy"
              decoding="async"
              draggable="false"
            />
          ) : (
            <span aria-hidden="true">{title}</span>
          )
        }
      </div>
    </div>
    <div class="project-card__body">
      <p>{summary}</p>
      {badge && <span class="project-card__badge">{badge}</span>}
      <ul class="project-card__tags" aria-label={`${title} technologies`}>
        {tags.map((tag) => <li>{tag}</li>)}
      </ul>
    </div>
  </article>
</li>
